# --- Core Data Science & Analysis ---
numpy<2.0.0
pandas<2.1.0
scipy==1.13.1
matplotlib==3.10.5
seaborn==0.13.2

# --- Machine Learning & Transformers ---
# PyTorch (torch)
# NOTE: This version is the CPU-compiled package.
# If you are using a GPU (CUDA), you may need to install the correct
# version directly from the PyTorch website (e.g., via conda or a specific pip command)
# to ensure CUDA compatibility, as requirements.txt is best for pure pip dependencies.
torch==2.5.1

# Scikit-learn (used for confusion matrix, train/test split)
scikit-learn==1.7.1

# Transformer Lens
# Note: latest compatible version via pip.
transformer-lens

# --- Utilities ---
# Included a few base libraries that are commonly required by the above packages
# that might not be correctly satisfied by a non-conda environment.
requests==2.32.5
transformers<5.0.0
